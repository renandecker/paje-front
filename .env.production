# Usado automaticamente pelo Vite quando roda "npm run build" (modo produção).
# Em desenvolvimento ("npm run dev"), essa variável não é lida — o front-end
# usa o proxy configurado em vite.config.js ('/api' -> backend local).
VITE_API_URL=https://paje-micro-service-1.onrender.com/api
